<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NeZha_Desktop.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border
            Margin="24"
            Padding="28"
            MaxWidth="760"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="16">

            <Grid ColumnSpacing="24" RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="1.8*" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="8">
                    <TextBlock Text="Nezha Desktop" Style="{ThemeResource TitleTextBlockStyle}" />
                    <TextBlock Text="登录并连接你的监控面板" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <TextBlock Text="支持多面板切换、自动登录、Token 自动刷新。" Opacity="0.7" TextWrapping="WrapWholeWords" />
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="12">
                    <ComboBox
                        Header="面板配置"
                        PlaceholderText="选择历史面板"
                        ItemsSource="{Binding Profiles}"
                        SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                        DisplayMemberPath="DashboardUrl" />

                    <TextBox
                        Header="Dashboard URL"
                        PlaceholderText="https://your-nezha.example.com"
                        Text="{Binding DashboardUrl, Mode=TwoWay}" />

                    <TextBox
                        Header="用户名"
                        PlaceholderText="admin"
                        Text="{Binding Username, Mode=TwoWay}" />

                    <PasswordBox x:ConnectionId='2'
                        x:Name="PasswordBox"
                        Header="密码"
                                                                        />

                    <CheckBox
                        Content="记住登录并启用自动登录"
                        IsChecked="{Binding RememberLogin, Mode=TwoWay}" />

                    <StackPanel Spacing="6">
                        <TextBlock Foreground="IndianRed" Text="{Binding ErrorMessage}" TextWrapping="WrapWholeWords" />
                        <TextBlock Foreground="SeaGreen" Text="{Binding InfoMessage}" TextWrapping="WrapWholeWords" />
                    </StackPanel>

                    <Button Content="登录" Command="{Binding LoginCommand}" HorizontalAlignment="Stretch" />
                </StackPanel>
            </Grid>
        </Border>

        <ProgressRing Grid.Row="1" Margin="0,0,0,24" HorizontalAlignment="Center" IsActive="{Binding IsBusy}" />
    </Grid>
</Page>

